/* CRITERIA:
Aggregation Query
Pick one query that requires the use of a distinct aggregation operator (min, max, average, or count are all fine).
*/

/* Returns the number of vaccines in the Vaccines table */
SELECT COUNT(*)
FROM Vaccine;